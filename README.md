# Hide Haven UI (Next.js)

## Getting started

```bash
npm install
npm run dev
```

Open http://localhost:3000 to view the home page.

## Multi-page routing

Add new routes under `app/`. Example:

- `app/collections/page.tsx` -> `/collections`
- `app/favorites/page.tsx` -> `/favorites`
